external printf(_: stringl) s32 = "printf"

syscall read(fd: s32, data: anyptr, size: u64) s64 = 0x01;

const PI = 3.14;

struct coordinate {
    x: f64,
    y: f64
}

fn test() s32 { 
    discard while (true) {
        $ empty
    };
    $ .some(10)
}